<{{ computed.tag }}{{ attributes }}>
    {% if computed.deferred %}
        <use xlink:href="#ux-icon-{{ computed.name }}" />
    {% else %}
        {{ computed.icon|raw }}
    {% endif %}
</{{ computed.tag }}>
