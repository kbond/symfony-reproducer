{% props form, name, help = false %}

<textarea{{ attributes.defaults({
    id: name,
    name: name,
    'aria-describedby': help ? name ~ '-help' : false,
    class: 'ui-form-input',
}) }}>{% block content %}{{ form.get(name) }}{% endblock %}</textarea>
