<input class="form-check-input {{ form.errorsFor(name) ? 'is-invalid' }}" type="checkbox" id="{{ id|default('form-' ~ name) }}" name="{{ name }}" {{ form.get(name) or default|default(false) ? 'checked' }} {{ disabled|default(false) ? 'disabled' }} {{ readonly|default(false) ? 'readonly' }} {{ required|default(false) ? 'required' }} />
{{ include('@ZenstruckFormRequest/bootstrap5/_label.html.twig', {type: 'checkbox'}) }}
{{ include('@ZenstruckFormRequest/bootstrap5/_help.html.twig') }}
